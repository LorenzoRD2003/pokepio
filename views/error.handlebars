<div class="container-sm mt-5">
    <div id="errorDiv" class="px-5 pt-3 bg-light mb-3 p-3 border">
        <h1>¡Ups! Ha ocurrido un error.</h1>

        {{#if error}}
        <div class="mt-3">
            <h3>Error {{error.code}}: {{error.message}}</h3>
        </div>
        {{/if}}

        <a class="btn btn-primary mt-3" href="/">Volver al inicio</a>
    </div>
</div>